apiVersion: v1
kind: Service
metadata:
  name: learnfast-api
  namespace: learnfast
spec:
  type: NodePort
  selector:
    app: learnfast-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
