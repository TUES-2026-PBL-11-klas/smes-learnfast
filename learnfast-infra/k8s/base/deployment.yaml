apiVersion: apps/v1
kind: Deployment
metadata:
  name: learnfast-api
  namespace: learnfast
  labels:
    app: learnfast-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: learnfast-api
  template:
    metadata:
      labels:
        app: learnfast-api
    spec:
      containers:
        - name: learnfast-api
          image: ststts/smes-learnfast:9bd753e9634a26c9f774ab576c34385fd2994472
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 15
